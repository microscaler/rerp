# BRRTRouter application configuration (YAML)
# Adjust values per environment and reload/restart the app.

security:
  api_keys:
    ApiKeyHeader:
      key: "test123"
http:
  keep_alive: true
  timeout_secs: 5
  max_requests: 5000
