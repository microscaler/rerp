service:
  name: bank-sync
image:
  repository: localhost:5001
  name: rerp-accounting-bank-sync
  tag: tilt
  pullPolicy: IfNotPresent
deployment:
  replicas: 1
  namespace: rerp
app:
  binaryName: bank_sync
  serviceName: bank-sync
