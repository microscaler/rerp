service:
  name: amd
image:
  repository: localhost:5001
  name: rerp-amd
  tag: tilt
  pullPolicy: IfNotPresent
deployment:
  replicas: 1
  namespace: rerp
app:
  binaryName: account_management_domain_service_api
  serviceName: amd
