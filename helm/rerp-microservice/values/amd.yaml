# ⚠️ PORT CONFIGURATION REFERENCE:
# Values file for AMD Service

service:
  name: amd
  port: 8004
  containerPort: 8004
  nodePort: 30804

image:
  repository: localhost:5001
  name: pricewhisperer-amd
  tag: tilt
  pullPolicy: IfNotPresent

deployment:
  replicas: 1
  namespace: pricewhisperer

app:
  binaryName: account_management_domain_service_api
  serviceName: amd

