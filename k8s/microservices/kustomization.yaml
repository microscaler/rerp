---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rerp

resources:
  - namespace.yaml
  # Accounting service configurations
  # Development environment configuration
  # Note: Service-specific configurations are managed via Helm charts
