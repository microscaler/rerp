# =============================================================================
# Rust (Cargo, rustfmt, IDE, coverage)
# =============================================================================
# Compiled output and build artifacts
target/
build/
debug/
build_artifacts/
./components/
._*
!*.__init*
BRRTRouter
# Remove Cargo.lock from gitignore if creating an executable; leave for libraries
# https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock
# The deployable microservices workspace is an application workspace and must
# resolve the same graph in CI, developer machines, and releases.
!microservices/Cargo.lock

# rustfmt backup files
**/*.rs.bk

# MSVC: debugging information
*.pdb

# Coverage (cargo-tarpaulin, cargo-llvm-cov)
cobertura.xml
tarpaulin-report.html
lcov.info
.lcov

# RustRover / JetBrains
#.idea/

# Release workflow artifact (generated by rerp release generate-notes)
release-body.md

# Local buildpack builder (machine-specific path)
.github/buildpack-builder-local.toml
PR_DESCRIPTION.md
PR_*.md
# =============================================================================
# Python (venv, bytecode, tests, tooling)
# =============================================================================
# Virtual environments
.venv/
venv/
env/
.env/
tooling/.venv/
tmp/
# Bytecode and caches
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python

# Distribution / packaging
*.egg
*.egg-info/
*.egg-info
.eggs/
tooling/**/*.egg-info/
dist/
*.manifest

# Coverage and pytest
.coverage
.coverage.*
htmlcov/
coverage.xml
.pytest_cache/

# Type checkers and linters
.mypy_cache/
.mypy_cache
.ruff_cache/

# Tox / nox
.tox/
.nox/

# Logs and local env (secrets)
**/*.log
**/*.logs
.env
.env.local
.env.*.local
pip-log.txt
pip-delete-this-directory.txt

# =============================================================================
# UI website (SolidJS build output, deps)
# =============================================================================
ui/website/dist/
ui/website/node_modules/
